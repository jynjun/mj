{
  "name": "@mj/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "MJ App Desktop (Electron) - reutilise l'export statique de @mj/web",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "start": "electron ."
  },
  "devDependencies": {
    "@mj/game-engine": "workspace:*",
    "@mj/storage": "workspace:*",
    "@mj/tsconfig": "workspace:*",
    "electron": "^33.2.1",
    "electron-builder": "^25.1.8",
    "typescript": "^5.7.2"
  }
}
